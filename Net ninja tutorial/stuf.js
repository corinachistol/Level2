//exportam variabila counter
export let counter = function(arr){
    //return "There are " + arr.length + " elements in this array"
    return `There are ${arr.length} elements in this array`
}


export let adder = function(a,b){
    return `The sum of the 2 numbers is ${a+b}`
}

export let pi = 3.142
